class PaymentPeriods {
    constructor(id, name, days, is_active, created_at) {
        this.id = id;
        this.name = name;
        this.days = days;
        this.is_active = is_active;
        this.created_at = created_at;
    }
}